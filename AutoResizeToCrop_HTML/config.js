var CONFIG = {
    /**
     * 基于当前缩放的缩放幅度
     * 取值范围[0,1]
     * 取值越大，计算量越大，速度越慢
     */
    scaleDuration: 0.02,
    /**
     * 基于当前位置的位置幅度
     * 取值范围[0,无穷大）
     * 取值越大，计算量越大，速度越慢
     */
    positionDuration: 5
}